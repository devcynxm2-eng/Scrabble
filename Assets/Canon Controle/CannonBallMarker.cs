using UnityEngine;

/// <summary>
/// Marks a spawned cannonball so PhysicsTowerObject can tell a real
/// hit apart from two tower blocks bumping into each other. No fields —
/// presence of the component is the signal.
/// </summary>
public sealed class CannonBallMarker : MonoBehaviour
{
}
